{
  "name": "oil-spill-detection-backend",
  "version": "1.0.0",
  "description": "Backend API for Oil Spill Detection System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test tests/*.test.js && python -m unittest discover -s tests -p \"test_*.py\" -v",
    "test:legacy": "node scripts/test_phase4b_characterization.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}